import React from "react";
import { motion } from "framer-motion";
import { useState } from "react";

function Modal({ setModalOpen }: { setModalOpen: () => void }) {
  return <div className="modal">모달창입니다.</div>;
}
export default Modal;
